# Elm Chat
A quick and dirty chat room written in Elm

## Setup
 - Use your own firebase creds, see appfb.js
 - Requires elm >= 0.18
 - `elm package install`
 - `npm install -g gulp`

## Running in dev mode
 - `gulp`
